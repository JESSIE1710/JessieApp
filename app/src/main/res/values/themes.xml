<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Jessappuno" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Jessappuno" parent="Base.Theme.Jessappuno" />

    <style name="boton_izquierdo">
        item<item name="android:layout_marginStart">@dimen/boton_lateral_izquierdo</item>
        item<item name="android:layout_alignStart">@dimen/texto_boton</item>
    </style>
    <style name="boton_derecho">
        item<item name="android:layout_marginStart">@dimen/boton_lateral_derecho</item>
    </style>

</resources>